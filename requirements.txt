# Python runtime
python>=3.10

# Core numeric / data
pandas>=2.0
numpy>=1.24
numpy-financial>=1.0

# Configuration (scenarios/*.yaml)
pyyaml>=6.0

# Plotting & results
matplotlib>=3.7

# Testing
pytest>=7.4

# Notebooks (workbench only; model modules do not depend on these)
jupyter>=1.0
