# High-gas-price scenario — overrides baselines.yaml
# Single-variable stress: delivered CNG cost at the top of a defensible Nigerian range.
# $8/MMBtu (baseline) -> $10.5/MMBtu. All other parameters identical to baselines.yaml.
fuel:
  price_usd_per_mmbtu: 10.5
